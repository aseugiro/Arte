@model IEnumerable<WebAppArte.Models.Artist>

@{
    ViewData["Title"] = "Artist";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
   <button name="button" onclick="ShowTable()">Mostrar información</button>
</div>

<h1>Artista</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new {  ID=item.ID, Name=item.Name, Country=item.Country, Description=item.Description }) |
                @Html.ActionLink("Detalles", "Index","Paint", new { ID=item.ID }) |
               
            </td>
        </tr>
}
    </tbody>
</table>

<script>
function ShowTable() {
    
    window.location = '@Url.Action("GetArtist", "Home")'; 
}

</script>